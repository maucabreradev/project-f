# 14 — Username rename

**What to build:** The member can replace the random username with a custom one exactly once; uniqueness is enforced.

**Blocked by:** 12 — Character creation

**Status:** ready-for-agent

- [ ] Exactly one rename per character; the new username is unique
- [ ] Mentions, search, and URLs resolve to the new username
