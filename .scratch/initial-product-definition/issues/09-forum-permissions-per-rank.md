# 09 — Forum permissions per rank

**What to build:** Read and write access configured per rank for each category and forum; permission checks enforced on the browsing and posting paths.

**Blocked by:** 07 — Admin dashboard shell, 08 — Board structure

**Status:** ready-for-agent

- [ ] Configuration UI per board per rank (read/write)
- [ ] Reading and writing enforce the configured permission per rank
